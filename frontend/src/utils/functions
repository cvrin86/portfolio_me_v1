
// functions.js (ou tout autre fichier où vous l'avez définie)
export const scrollToSection = (id) => {
    const section = document.getElementById(id);
    if (section) {
        section.scrollIntoView({ behavior: 'auto' });
    }
};
